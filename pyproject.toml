[project]
name = "movie-chatbot"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
moviebot = "src.main:run"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["src"]
